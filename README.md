import pandas as pd

# предположим, что у тебя уже есть:
# df_main  – основная таблица (есть поле "ид должности")
# df_add   – доп. таблица (есть поля "ид должности", "раб.время", "фио сотрудника", "таб.номер")

# 1. Оставляем в доп. таблице только строки, где рабочее время = 100
df_add_100 = df_add[df_add['раб.время'] == 100].copy()

# 2. Оставляем только нужные поля для подтягивания
df_add_100 = df_add_100[['ид должности', 'фио сотрудника', 'таб.номер']]

# 3. Мержим в основную таблицу по ключу "ид должности"
df_main = df_main.merge(
    df_add_100,
    on='ид должности',
    how='left'
)

# результат:
# в df_main появятся колонки "фио сотрудника" и "таб.номер"
# только для тех "ид должности", где в доп. таблице было раб.время = 100
