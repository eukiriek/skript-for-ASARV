Методология моделей расчёта предиктивного анализа
Модель Prophet

Prophet — алгоритм прогнозирования временных рядов, разработанный компанией Meta (Facebook), предназначенный для работы с нестационарными временными рядами, содержащими тренды, сезонность и календарные эффекты.
Модель устойчива к пропускам в данных, выбросам и изменениям тренда.

Общая форма модели Prophet:

𝑦
(
𝑡
)
=
𝑔
(
𝑡
)
+
𝑠
(
𝑡
)
+
ℎ
(
𝑡
)
+
𝜀
𝑡
y(t)=g(t)+s(t)+h(t)+ε
t
	​


где:

g(t) — трендовая компонента (линейная или логистическая),

s(t) — сезонная компонента (годовая, недельная, дневная и др.),

h(t) — эффекты праздников и событий,

εₜ — случайная ошибка модели.

Процесс расчёта модели Prophet
1. Подготовка данных

Для работы алгоритма Prophet временной ряд приводится к стандартному формату:

ds — временная метка (дата),

y — целевая переменная.

При необходимости выполняется:

агрегация данных по временному шагу (день, неделя, месяц),

обработка пропущенных значений,

выявление и сглаживание экстремальных выбросов.

2. Анализ структуры временного ряда

На данном этапе проводится предварительный анализ данных:

выявление долгосрочного тренда,

определение сезонных компонент,

оценка возможных структурных сдвигов во времени.

В отличие от ARIMA, приведение ряда к стационарному виду не требуется.

3. Разделение данных

Временной ряд делится на:

обучающую выборку — 80% наблюдений,

тестовую выборку — 20% наблюдений.

Разделение осуществляется по хронологическому принципу.

4. Настройка параметров модели

На обучающей выборке задаются основные параметры модели:

тип тренда (линейный / логистический),

точки изменения тренда (changepoints),

параметры сезонности (additive / multiplicative),

коэффициенты регуляризации:

changepoint_prior_scale,

seasonality_prior_scale.

При необходимости добавляются:

пользовательские сезонности,

праздничные и событийные эффекты.

5. Обучение модели

Модель Prophet обучается на обучающей выборке с учётом заданных параметров и выявленных компонент временного ряда.

6. Диагностика модели

После обучения проводится оценка адекватности модели:

анализ остатков,

проверка стабильности тренда,

анализ вклада сезонных и событийных компонент.

7. Оценка качества прогноза

Качество прогноза на тестовой выборке оценивается с использованием метрик:

MAE (Mean Absolute Error),

MAPE (Mean Absolute Percentage Error),

RMSE (Root Mean Squared Error).

8. Построение прогноза

На финальном этапе формируется прогноз на заданный горизонт времени с расчётом:

точечного прогноза,

доверительных (прогнозных) интервалов.

Результат прогноза может быть представлен в виде:

временного графика,

таблицы прогнозных значений,

декомпозиции на тренд, сезонность и эффекты событий.

Если хочешь, могу дальше:

привести сравнение ARIMA / SARIMA vs Prophet в табличном виде,

адаптировать текст под HR-метрики (текучесть, найм, absenteeism),

сократить до методологии для 1–2 слайдов или корпоративного отчёта.
