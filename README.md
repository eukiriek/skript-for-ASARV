Методология моделей расчёта предиктивного анализа
Экспоненциальное сглаживание и модель Хольта—Винтерса
1. Экспоненциальное сглаживание (Exponential Smoothing)

Экспоненциальное сглаживание — класс методов прогнозирования временных рядов, основанный на взвешивании прошлых наблюдений, где более свежие значения имеют больший вес. Метод эффективен для рядов без выраженного тренда и сезонности.

Общая форма простой модели экспоненциального сглаживания (SES):
𝑦
^
𝑡
=
𝛼
𝑦
𝑡
+
(
1
−
𝛼
)
𝑦
^
𝑡
−
1
y
^
	​

t
	​

=αy
t
	​

+(1−α)
y
^
	​

t−1
	​


где:

ŷₜ — сглаженное значение временного ряда,

yₜ — фактическое значение временного ряда,

α — коэффициент сглаживания (0 < α ≤ 1).

2. Модель Хольта (Holt’s Linear Trend)

Модель Хольта расширяет простое экспоненциальное сглаживание за счёт добавления линейного тренда.

𝑙
𝑡
	
=
𝛼
𝑦
𝑡
+
(
1
−
𝛼
)
(
𝑙
𝑡
−
1
+
𝑏
𝑡
−
1
)


𝑏
𝑡
	
=
𝛽
(
𝑙
𝑡
−
𝑙
𝑡
−
1
)
+
(
1
−
𝛽
)
𝑏
𝑡
−
1


𝑦
^
𝑡
+
ℎ
	
=
𝑙
𝑡
+
ℎ
𝑏
𝑡
l
t
	​

b
t
	​

y
^
	​

t+h
	​

	​

=αy
t
	​

+(1−α)(l
t−1
	​

+b
t−1
	​

)
=β(l
t
	​

−l
t−1
	​

)+(1−β)b
t−1
	​

=l
t
	​

+hb
t
	​

	​


где:

lₜ — уровень ряда,

bₜ — тренд,

α, β — коэффициенты сглаживания.

3. Модель Хольта—Винтерса (Holt–Winters)

Модель Хольта—Винтерса применяется для временных рядов с трендом и сезонностью.
Существует две модификации модели:

аддитивная — при постоянной амплитуде сезонности,

мультипликативная — при сезонности, зависящей от уровня ряда.

Аддитивная модель Хольта—Винтерса:
𝑙
𝑡
	
=
𝛼
(
𝑦
𝑡
−
𝑠
𝑡
−
𝑚
)
+
(
1
−
𝛼
)
(
𝑙
𝑡
−
1
+
𝑏
𝑡
−
1
)


𝑏
𝑡
	
=
𝛽
(
𝑙
𝑡
−
𝑙
𝑡
−
1
)
+
(
1
−
𝛽
)
𝑏
𝑡
−
1


𝑠
𝑡
	
=
𝛾
(
𝑦
𝑡
−
𝑙
𝑡
)
+
(
1
−
𝛾
)
𝑠
𝑡
−
𝑚


𝑦
^
𝑡
+
ℎ
	
=
𝑙
𝑡
+
ℎ
𝑏
𝑡
+
𝑠
𝑡
+
ℎ
−
𝑚
l
t
	​

b
t
	​

s
t
	​

y
^
	​

t+h
	​

	​

=α(y
t
	​

−s
t−m
	​

)+(1−α)(l
t−1
	​

+b
t−1
	​

)
=β(l
t
	​

−l
t−1
	​

)+(1−β)b
t−1
	​

=γ(y
t
	​

−l
t
	​

)+(1−γ)s
t−m
	​

=l
t
	​

+hb
t
	​

+s
t+h−m
	​

	​


где:

sₜ — сезонная компонента,

m — длина сезонного цикла,

α, β, γ — коэффициенты сглаживания.

Процесс расчёта моделей экспоненциального сглаживания и Хольта—Винтерса
1. Предварительный анализ временного ряда

На первом этапе проводится анализ структуры временного ряда:

наличие тренда,

наличие сезонности,

стабильность дисперсии.

Приведение ряда к стационарному виду не требуется.

2. Подготовка данных

Выполняется:

агрегация данных по выбранному временному интервалу,

обработка пропусков,

устранение аномальных выбросов (при необходимости).

3. Разделение данных

Временной ряд разделяется на:

обучающую выборку — 80% наблюдений,

тестовую выборку — 20% наблюдений.

Разделение выполняется строго по временной последовательности.

4. Выбор модели

В зависимости от структуры данных выбирается модель:

простое экспоненциальное сглаживание — при отсутствии тренда и сезонности,

модель Хольта — при наличии тренда,

модель Хольта—Винтерса — при наличии тренда и сезонности.

Также определяется тип сезонности:

аддитивная,

мультипликативная.

5. Подбор параметров

Параметры сглаживания α, β, γ подбираются автоматически путём минимизации функции ошибки (например, MSE) на обучающей выборке.

6. Обучение модели

На обучающей выборке производится оценка параметров и обучение выбранной модели.

7. Диагностика модели

Проводится:

анализ остатков,

проверка отсутствия автокорреляции,

оценка устойчивости трендовой и сезонной компонент.

8. Оценка качества прогноза

На тестовой выборке рассчитываются метрики:

MAE,

MAPE,

RMSE.

9. Построение прогноза

Формируется прогноз на заданный горизонт времени с расчётом:

точечного прогноза,

доверительных интервалов.

Результаты могут быть представлены в виде:

временного графика,

таблицы прогнозных значений,

декомпозиции уровня, тренда и сезонности.

Если нужно, дальше могу:

собрать единый методологический блок (ARIMA / Prophet / Holt-Winters) для отчёта,

сделать таблицу выбора модели в зависимости от типа временного ряда,

адаптировать текст под HR-метрики и бизнес-кейсы.
