Модель Каплана—Мейера (Kaplan–Meier Estimator)

Модель Каплана—Мейера — непараметрический метод анализа выживаемости, предназначенный для оценки вероятности «выживания» объекта во времени до наступления события.
Метод широко используется для анализа времени до увольнения сотрудника, срока жизни кандидатов, времени до отказа, оттока клиентов и аналогичных событий.

Общая форма модели

Оценка функции выживаемости определяется как:

𝑆
^
(
𝑡
)
=
∏
𝑡
𝑖
≤
𝑡
(
1
−
𝑑
𝑖
𝑛
𝑖
)
S
^
(t)=
t
i
	​

≤t
∏
	​

(1−
n
i
	​

d
i
	​

	​

)

где:

Ŝ(t) — оценка функции выживаемости в момент времени t,

tᵢ — момент наступления события,

dᵢ — количество событий в момент tᵢ,

nᵢ — количество объектов «под риском» непосредственно перед моментом tᵢ.

Основные понятия модели

Событие — факт, наступление которого анализируется
(например, увольнение сотрудника).

Цензурирование — ситуация, когда событие не произошло до окончания периода наблюдения
(например, сотрудник продолжает работать).

Время наблюдения — период от начальной точки до события или цензурирования.

Процесс расчёта модели Каплана—Мейера
1. Формирование набора данных

Для каждого наблюдения формируются следующие параметры:

дата начала наблюдения (например, дата найма),

дата окончания наблюдения (дата увольнения или конец периода анализа),

индикатор события:

1 — событие произошло,

0 — наблюдение цензурировано.

2. Расчёт времени до события

Для каждого объекта вычисляется:

𝑇
𝑖
=
Дата окончания
−
Дата начала
T
i
	​

=Дата окончания−Дата начала

Время приводится к единой шкале:

дни,

недели,

месяцы.

3. Группировка наблюдений (опционально)

При необходимости данные сегментируются по признакам:

источник кандидата,

подразделение,

должность,

регион,

формат занятости и др.

Для каждой группы строится отдельная функция выживаемости.

4. Сортировка данных

Наблюдения сортируются по возрастанию времени до события.
Цензурированные наблюдения учитываются в расчёте количества объектов под риском, но не уменьшают вероятность выживания.

5. Расчёт функции выживаемости

На каждом моменте наступления события:

определяется количество объектов под риском nᵢ,

определяется количество событий dᵢ,

вычисляется шаг функции выживаемости.

Функция имеет ступенчатый вид.

6. Визуализация результатов

Результат расчёта представляется в виде:

кривой выживаемости Каплана—Мейера,

сравнения кривых между группами,

доверительных интервалов (обычно 95%).

7. Оценка статистической значимости

Для сравнения кривых выживаемости между группами применяется:

Log-Rank тест.

Он позволяет определить, являются ли различия статистически значимыми.

8. Интерпретация результатов

На основе кривой выживаемости можно определить:

вероятность сохранения объекта к заданному моменту времени,

медиану времени жизни (момент, когда Ŝ(t) = 0.5),

интервалы повышенного риска наступления события.

9. Использование в предиктивной аналитике

Результаты модели применяются для:

оценки риска увольнения сотрудников,

сравнения источников найма,

анализа эффективности онбординга,

поддержки решений в HR-стратегии и workforce planning.

Если нужно, могу дополнительно:

переписать методологию строго под HR-контекст (текучесть, кандидаты, адаптация),

добавить блок ограничений и предпосылок модели,

подготовить единый методологический раздел со всеми моделями (ARIMA / Prophet / Holt-Winters / Kaplan–Meier),

сделать версию для презентации (1–2 слайда).
