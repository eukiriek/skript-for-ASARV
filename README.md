Логистическая регрессия (Logistic Regression)

Логистическая регрессия — статистическая модель бинарной классификации, предназначенная для оценки вероятности наступления события.
Модель широко применяется для прогнозирования увольнения сотрудника, выхода кандидата, отклика на оффер, вероятности ухода клиента и других бинарных исходов.

Общая форма модели

Вероятность наступления события определяется логистической функцией:

𝑃
(
𝑌
=
1
∣
𝑋
)
=
1
1
+
𝑒
−
(
𝛽
0
+
𝛽
1
𝑋
1
+
⋯
+
𝛽
𝑘
𝑋
𝑘
)
P(Y=1∣X)=
1+e
−(β
0
	​

+β
1
	​

X
1
	​

+⋯+β
k
	​

X
k
	​

)
1
	​


где:

Y — бинарная целевая переменная:

1 — событие произошло,

0 — событие не произошло,

X₁ … Xₖ — независимые переменные (факторы),

β₀ — свободный член,

β₁ … βₖ — коэффициенты модели.

Логарифм отношения шансов (log-odds):

log
⁡
(
𝑃
(
𝑌
=
1
)
1
−
𝑃
(
𝑌
=
1
)
)
=
𝛽
0
+
∑
𝑖
=
1
𝑘
𝛽
𝑖
𝑋
𝑖
log(
1−P(Y=1)
P(Y=1)
	​

)=β
0
	​

+
i=1
∑
k
	​

β
i
	​

X
i
	​

Процесс расчёта модели логистической регрессии
1. Формирование целевой переменной

Определяется бинарная целевая переменная:

1 — событие произошло (например, сотрудник уволился),

0 — событие не произошло.

Фиксируется период наблюдения (например, 3, 6 или 12 месяцев).

2. Подготовка признаков

Формируется набор независимых переменных, например:

демографические характеристики,

условия труда,

компенсация и бенефиты,

показатели эффективности,

факторы нагрузки и стресса.

Проводится:

обработка пропусков,

кодирование категориальных переменных (one-hot encoding),

масштабирование числовых признаков (при необходимости).

3. Анализ мультиколлинеарности

Перед обучением модели проводится анализ корреляций между признаками:

корреляционная матрица,

показатель VIF (Variance Inflation Factor).

Высококоррелированные признаки при необходимости исключаются или агрегируются.

4. Разделение данных

Данные разделяются на:

обучающую выборку — 70–80%,

тестовую выборку — 20–30%.

Разделение выполняется с сохранением доли классов (stratified split).

5. Обучение модели

На обучающей выборке производится оценка коэффициентов логистической регрессии методом максимального правдоподобия.

При необходимости используется регуляризация:

L1 (Lasso) — отбор признаков,

L2 (Ridge) — снижение переобучения.

6. Оценка качества модели

Качество классификации оценивается с использованием:

ROC-AUC,

accuracy,

precision / recall,

F1-score,

confusion matrix.

Дополнительно анализируется калибровка вероятностей.

7. Интерпретация результатов

Коэффициенты модели интерпретируются через:

знак и величину β,

отношение шансов (odds ratio):

𝑂
𝑅
𝑖
=
𝑒
𝛽
𝑖
OR
i
	​

=e
β
i
	​


Это позволяет оценить влияние факторов на вероятность наступления события.

8. Валидация и устойчивость модели

Проводится:

кросс-валидация,

проверка стабильности коэффициентов во времени,

анализ качества модели на новых периодах данных.

9. Использование модели

Результаты модели применяются для:

расчёта индивидуальной вероятности события,

сегментации сотрудников по уровню риска,

приоритизации управленческих действий,

поддержки решений в HR-стратегии и workforce planning.

10. Ограничения модели

линейная зависимость логитов от признаков,

чувствительность к выбросам,

ограниченная способность моделировать сложные нелинейные эффекты.

Если хочешь, могу дополнительно:

связать логистическую регрессию с Kaplan–Meier (time-to-event vs probability),

адаптировать под скоринговую модель риска увольнения,

добавить пример интерпретации коэффициентов для HR,

собрать единый методологический раздел со всеми моделями в одном документе.
